import React from 'react';
import { BeatLoader } from 'react-spinners';

const Loader = () => (
  <div className="Loader">
    <BeatLoader color="#303f9f" loading />
  </div>
);

export default Loader;
